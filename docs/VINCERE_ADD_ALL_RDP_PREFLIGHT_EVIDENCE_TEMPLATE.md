# Vincere Add All RDP Preflight Evidence Template

Date:
Operator:
Kush approval reference:

## Scope

Use this template only for the next Kush-approved visible/unlocked RDP preflight before any Add All retest.

This template does not approve Add All. Add All requires a separate explicit Kush approval after this preflight evidence is recorded.

## Required Approval Gate

```text
Kush, do you approve a supervised visible/unlocked RDP preflight only, to confirm NinjaTrader is running, READY_ALGOS_ENABLE_STRATEGIES=false, OrdersGrid=0, PositionsGrid=0, all visible strategy rows disabled, the exact target account set, and baseline StrategiesGrid, with no Add All click/run until you separately approve it after the preflight evidence is recorded?
```

Approval response:

## Git Evidence

Before preflight:

```text
git status --short --branch =
```

After preflight:

```text
git status --short --branch =
```

## Latest Readiness Evidence

Latest readiness report path:

```text
logs\production-readiness\20260607-180934\PRODUCTION_READINESS_REPORT.md
```

Latest known readiness summary:

```text
PASS=40
FAIL=0
WARN=2
SKIP=10
Add All skipped because -IncludeLiveAddAll was not used
NinjaTrader process not found during that harness
```

## Preflight Safety State

Record exact values before any Add All approval request:

```text
READY_ALGOS_ENABLE_STRATEGIES=false
Desktop visible/unlocked=<yes/no>
NinjaTrader running=<yes/no>
OrdersGrid=0
PositionsGrid=0
All visible strategy rows disabled=<yes/no>
Target account set=<exact Kush-approved accounts>
StrategiesGrid=<baseline count>
No stale/leftover strategy rows beyond approved baseline=<yes/no>
```

Evidence location or screenshot references:

```text

```

## Stop Conditions

Stop immediately and do not request Add All approval if any item is true:

- `READY_ALGOS_ENABLE_STRATEGIES` is not exactly `false`.
- NinjaTrader is not running.
- `OrdersGrid` is nonzero, unreadable, or uncertain.
- `PositionsGrid` is nonzero, unreadable, or uncertain.
- Any visible strategy row is enabled.
- Visible strategy enabled/disabled state is unreadable or uncertain.
- Target account set is missing, incomplete, or not explicitly approved by Kush.
- Baseline `StrategiesGrid` is missing, unreadable, or uncertain.
- Stale/leftover strategy rows are present beyond the Kush-approved baseline, or stale-row status is unreadable or uncertain.
- Desktop is not visible/unlocked.
- Any unexpected order, position, connection, or strategy state appears.

## Artifacts To Preserve

- This completed evidence template.
- Latest readiness report: `logs\production-readiness\20260607-180934\PRODUCTION_READINESS_REPORT.md`.
- Any new supervised preflight screenshots or report folder under `logs\production-readiness\<timestamp>\`.
- `git status --short --branch` before and after the preflight.
- If a later separately approved Add All run occurs: `logs\stack-apply-batches\*.json`.
- If a later separately approved Add All run occurs: stack apply log output.
- If generated by a later separately approved Add All run: `logs\add-all-diagnostics\<batchId>\` or `%TEMP%\vincere-add-all-diagnostics\<runId>\`.

## Explicitly Forbidden During This Preflight

- Do not run Add All.
- Do not pass or use `-IncludeLiveAddAll`.
- Do not enable strategies.
- Do not disable strategies unless Kush separately approves an emergency supervised safety action.
- Do not delete, remove, reconcile, or clean up strategy rows.
- Do not change scheduled tasks.
- Do not deploy.
- Do not push to GitHub.
- Do not use secrets or make network calls.
- Do not change production behavior.
- Do not change `READY_ALGOS_ENABLE_STRATEGIES=true`.
- Do not run browser/RDP/NinjaTrader UI automation outside the exact Kush-approved visible/unlocked preflight.

## Post-Preflight Decision

```text
Preflight passed=<yes/no>
If no, blocker=
Confirm no Add All was run during preflight=<yes/no>
Confirm no readiness harness was run during preflight=<yes/no>
Confirm no strategy enable/disable was performed=<yes/no>
Confirm no row cleanup/deletion was performed=<yes/no>
If yes, exact second approval question for Add All=
```
